h1.heading-xlarge Details of your jury service
p.lede You have been selected to go to court on the date and time below.
.panel-indent
  p 
    | {{ vm.juror.name }}
    br
    | {{ vm.juror.address }}
  p.bold-small
    span(ng-bind='vm.juror.court.name')
    br
    span(ng-bind='vm.juror.court.address')
  p.bold-small {{ vm.juror.datetime | date:'HH.mma on EEEE d MMMM yyyy'}}
hr.line
form.form(name='profileForm', ng-submit='vm.submit()', novalidate)
  fieldset
    legend.form-label-bold How would you like to respond?
    .form-group
      label.block-label(for='accept')
        input(type='radio', value='accept', id='accept', name='response', ng-model='vm.formData.response')
        strong Accept this date and complete your form
      label.block-label(for='delay')
        input(type='radio', value='delay', id='delay', name='response', ng-model='vm.formData.response')
        strong Delay until a later date
        br
        span You can rearrange your jury service if you can’t make the date we’ve given you.
      label.block-label(for='excuse')
        input(type='radio', value='excuse', id='excuse', name='response', ng-model='vm.formData.response')
        strong Apply to be excused
        br
        span We can only release you from jury service if you have a good reason why you can’t do it in the next 12 months.
  fieldset
    .form-group-compound
      input.button(type='submit', value='Continue')
hr.line
section
  h2.bold-medium Get details about the court
  p Find out how to get there, when it’s open, and what facilities are on offer.
  p
    a(href='{{ vm.juror.court.url }}', rel='external') Find out more about {{ vm.juror.court.name }}
